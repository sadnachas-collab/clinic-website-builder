## Что не так

В логотипе сейчас написано **VORONÉNKO** (с акцентом É). Визуально это читается как «ВОРОНЕЕНКО». Правильно — **VORONENKO** (просто E без акцента).

## Что поправлю

Заменю `VORONÉNKO` → `VORONENKO` во всех местах, где встречается в проекте:

1. `public/clinic-site.html`
   - `<title>` страницы
   - логотип в шапке (строка ~129)
   - логотип в футере (строка ~996)

2. `public/clinic-admin.html`
   - `<title>` страницы
   - логотип в шапке админки (строка ~57)

3. `src/routes/admin.tsx`
   - `<title>` мета-тега роута `/admin`

Строка `© 2026 Клиника Dr. VORONENKO` уже написана правильно — её не трогаю.

## Чего НЕ делаю

- Не меняю дизайн, шрифты, размеры, цвета, разметку.
- Не трогаю JS-логику, авторизацию (admin / 123456 продолжает работать).
- Не подключаю базу данных и Lovable Cloud.
- Не правлю файлы в `docs/source/` — это исходники из конвертации, на работающий сайт не влияют.
